def main |a int| -> void {}
def some || -> void {}
def another |a float, b int| -> int {
    # some function here
    a float = 3.0;
    b int;
    loop a, a..100.0 {}
    loop {
        if b > a next;
        else if b == [some] stop;
        else ! 32.0;
    }
    if a * 38 == 4.0 and __some_var_228 + [another 15, 32.0] {
        a int = not 308 and 3 * 4 != 3 / (1000 and not 3);
    }
}